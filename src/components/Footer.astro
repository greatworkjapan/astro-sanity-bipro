---
const footerLinks = {
  main: [
    { name: '美容機器', href: '/devices' },
    { name: 'サロン商材', href: '/products' },
    { name: '経営・ノウハウ', href: '/management' },
    { name: '開業ガイド', href: '/startup' },
  ],
  sub: [
    { name: '当サイトについて', href: '/about' },
    { name: 'お問い合わせ', href: '/contact' },
    { name: '資料請求', href: '/request' },
    { name: 'サイトマップ', href: '/sitemap' },
  ],
  legal: [
    { name: 'プライバシーポリシー', href: '/privacy' },
    { name: '利用規約', href: '/terms' },
  ],
};
---
<footer class="bg-gray-800 text-white">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <div class="grid grid-cols-2 md:grid-cols-4 gap-8">
      <div>
        <h3 class="text-sm font-semibold tracking-wider uppercase">メインカテゴリ</h3>
        <ul class="mt-4 space-y-2">
          {footerLinks.main.map(link => (
            <li><a href={link.href} class="text-base text-gray-300 hover:text-white">{link.name}</a></li>
          ))}
        </ul>
      </div>
      <div>
        <h3 class="text-sm font-semibold tracking-wider uppercase">サポート</h3>
        <ul class="mt-4 space-y-2">
          {footerLinks.sub.map(link => (
            <li><a href={link.href} class="text-base text-gray-300 hover:text-white">{link.name}</a></li>
          ))}
        </ul>
      </div>
       <div>
        <h3 class="text-sm font-semibold tracking-wider uppercase">リーガル</h3>
        <ul class="mt-4 space-y-2">
          {footerLinks.legal.map(link => (
            <li><a href={link.href} class="text-base text-gray-300 hover:text-white">{link.name}</a></li>
          ))}
        </ul>
      </div>
      <div class="col-span-2 md:col-span-1">
         <h3 class="text-lg font-bold">Salon Biz Support</h3>
         <p class="mt-2 text-gray-400">サロン運営者のための専門情報サイト</p>
      </div>
    </div>
    <div class="mt-8 border-t border-gray-700 pt-8 text-center text-base text-gray-400">
      &copy; {new Date().getFullYear()} Salon Biz Support. All rights reserved.
    </div>
  </div>
</footer>
